import React from 'react';

// 문자열 압축

const StringCompression = () => {
  const solution = (s) => {
    let answer = 0;
    return answer;
  }
  return (
    <div>

    </div>
  );
};

export default StringCompression;
