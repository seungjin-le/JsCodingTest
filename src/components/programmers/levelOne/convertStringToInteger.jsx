import React from 'react';

// 문자열을 정수로 바꾸기

const ConvertStringToInteger = () => {
  const solution = (s) => {
    return parseInt(s);
  }
  return (
    <div>
      
    </div>
  );
};

export default ConvertStringToInteger;
